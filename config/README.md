# Configuration

`milky_cow_study_contract.json` is the active scope lock.

`legacy_25k_baseline.json` is an exact parent-project snapshot. It contains
useful rule and cohort fields, but also parent inventory/purchase controls. Do
not load it wholesale as the new project's baseline; create a new reviewed
config after the open contracts are decided.

`payout_policies.json` is an exact six-policy candidate set. Its policies must
be retested under synchronized copy-to-all exposure.
