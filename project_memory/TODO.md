# TODO

- [x] Verify the transfer before code changes and record intentional deviations.
- [x] Audit inherited rules and input contracts against copy-to-all.
- [x] Verify RR1 and lock the 9,299/3,359 whole-tape PA stream.
- [x] Execute the three pinned Evaluation behavior-lock episodes.
- [x] Lock copy-to-all N=1/2/3, activation exclusion, correlated deaths, and
      exact-exit one-shot settlement.
- [x] Define executable scaling schedule/scope primitives without selecting one.
- [x] Define pipeline-cap, acquisition, death-count replacement, and external-
      capital primitives without selecting policies.
- [x] Bind selective derivatives and active evidence by SHA-256.
- [x] Define the headline PA-count estimand as maintained N acquired from zero,
      with active/running/pending commitments capped at N.
- [x] Promote completed-trade path order to three Evaluation-and-PA scenarios
      and bind the RR1 ambiguity population and seeded assignment.
- [x] Implement and test the smallest deterministic N=1 lifecycle plus an N=2
      divergence trace, without treating fixture policies as study baselines.
- [x] Select the Evaluation consumer: the parent's cycle-local one-position
      adapter, re-selected at every renewal boundary, at 3 MNQ.
- [x] Select the phase-1 MNQ schedule (flat 1 MNQ, no scaling), close-only
      commission timing, and declare execution/slippage a non-model with its
      bias direction recorded.
- [x] Select acquisition (greedy in time, one Evaluation per decision) and
      replacement (emergent from the pipeline cap, not an independent policy).
- [x] Select starting cash ($35), capital mode (through-first-PA bridge), no
      lifetime budget, and skip-the-purchase unfunded behavior.
- [x] Bind the parent comparison arm, including the mae_first path arm to
      difference against and the K=5 value explicitly not adopted.
- [x] Resolve the Evaluation rule boundaries: renewal only below the cap,
      dormant-to-boundary mid-cycle failure, Tallinn 00:00 trading day.
- [x] Resolve open-position handling: no trading on a payout day.
- [x] Define the objective (net cash withdrawn, net of external capital),
      horizon (720 primary / 1,440 diagnostic), censoring (withdrawn and
      un-withdrawn reported separately), monthly cohorts, and regimes.
- [x] Declare prop-firm failure and rule-change stresses non-models.
- [x] Resolve the payout lifecycle: parent-parity atomic timing, terminal sweep
      as censoring valuation, six candidates unchanged.
- [x] Confirm the dormant Evaluation holds its pipeline slot (parent-verified).
- [x] Resolve same-timestamp event order and bind `_PHASE_RANK` to the gate's
      declared order by test.
- [ ] Confirm the accumulation-trigger exclusion; it is a recommendation on
      measured evidence, not a user decision.
- [ ] Implement the study-scale lifecycle runner; only the N=1/N=2 fixture
      slice exists.
- [ ] Run the ordering-sensitivity arm on selected N with a permuted order.
- [ ] Only after every gate field is resolved, run the 1..20 x six-payout policy
      study and report rolling cohorts/regimes/stress without inherited claims.
