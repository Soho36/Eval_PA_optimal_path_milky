# Processed inputs

Deterministic normalization outputs belong here and are ignored by Git. Each
file must be reproducible from raw inputs and versioned transformation code.
Validation should reject duplicate trade keys, non-chronological events, missing
economic fields and unsupported rule intervals.
