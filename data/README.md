# Data

`raw/rr1/` is the complete frozen RR=1 completed-trade tape copied from the
parent repository. Verify it through `manifests/rr1_import_20260829.json` before
use.

`INPUT_CONTRACTS_FROM_PARENT.md` records the parent data contract. Audit and
replace it with a milky-cow-specific input contract before the integrated
simulator begins.
